import React from 'react'

export default function SaveSettings() {
  return (
    <div>SaveSettings</div>
  )
}


// an object should be passed into this function which are all the settings being changed, and the function should update the settings in local storage and firebase
// The state will still have to be updated in the parent component

function saveSettings(settings){
    // save settings to local storage
    
    // save settings to firebase
    // update settings in parent component
}